<% @page_title = "記事一覧" %>

<% if @articles.present? %>
	<% @articles.each do |article| %>
		<%= link_to article.title, article %> |
		<%= article.created_at %>
	<% end %>
<% else %>
	記事はありません
<% end %>