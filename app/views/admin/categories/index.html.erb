<% @page_title = "カテゴリ一覧" %>
<p id="notice"><%= notice %></p>
<h1><%= @page_title %></h1>

<div>
<% @categories.each do |category| %>
	<%= link_to category.name, [:admin, category] %> | 
	<%= link_to "編集", [:edit, :admin, category] %> |
	<%= link_to "削除", [:admin, category], method: :delete, data: {confirm: "削除しますか"} %><br>
<% end %>
</div>
<%= link_to "新規作成", new_admin_category_path %>